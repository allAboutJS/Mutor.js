export enum MODE {
	CODE,
	TEXT,
}

export enum TOKEN_TYPE {
	STRING_LITERAL,
	PARENTHESES,
	KEYWORD,
	TEXT,
	IDENTIFIER,
	AT_IDENTIFIER,
	DOT,
	COLON,
	COMMA,
	NUMERIC_LITERAL,
	WHITESPACE,
	OPERATOR,
	CODE_START,
	CODE_END,
}

export enum NODE_TYPE {
	BINARY_EXPRESSION,
	IDENTIFIER,
	MEMBER_EXPRESSION,
	NUMERIC_LITERAL,
	STRING_LITERAL,
	TEXT,
}
